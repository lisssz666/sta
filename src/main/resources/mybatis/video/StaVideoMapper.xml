<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.project.video.mapper.StaVideoMapper">
    
    <resultMap type="StaVideo" id="StaVideoResult">
        <result property="id"    column="id"    />
        <result property="fileName"    column="file_name"    />
        <result property="filePath"    column="file_path"    />
        <result property="leagueId"    column="league_id"    />
        <result property="gameId"    column="game_id"    />
        <result property="type"    column="type"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectStaVideoVo">
        select id, file_name, file_path,league_id, game_id, type, create_by, create_time, update_by, update_time from sta_video
    </sql>

    <select id="selectStaVideoList" parameterType="StaVideo" resultMap="StaVideoResult">
        <include refid="selectStaVideoVo"/>
        <where>  
            <if test="fileName != null  and fileName != ''"> and file_name like concat('%', #{fileName}, '%')</if>
            <if test="filePath != null  and filePath != ''"> and file_path = #{filePath}</if>
            <if test="leagueId != null  and leagueId != ''"> and league_id = #{leagueId}</if>
            <if test="gameId != null  and gameId != ''"> and game_id = #{gameId}</if>
            <if test="type != null  and type != ''"> and type = #{type}</if>
        </where>
        <choose>
            <when test="type == 2">
                ORDER BY create_time DESC LIMIT 1
            </when>
            <otherwise>
                <!-- 当 type 不等于 2 时，不添加任何额外的排序或限制 -->
            </otherwise>
        </choose>
    </select>
    
    <select id="selectStaVideoById" parameterType="Long" resultMap="StaVideoResult">
        <include refid="selectStaVideoVo"/>
        where id = #{id}
    </select>
        
    <insert id="insertStaVideo" parameterType="StaVideo" useGeneratedKeys="true" keyProperty="id">
        insert into sta_video
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="fileName != null">file_name,</if>
            <if test="filePath != null">file_path,</if>
            <if test="type != null">type,</if>
            <if test="leagueId != null">league_id,</if>
            <if test="gameId != null">game_id,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="fileName != null">#{fileName},</if>
            <if test="filePath != null">#{filePath},</if>
            <if test="type != null">#{type},</if>
            <if test="leagueId != null">#{leagueId},</if>
            <if test="gameId != null">#{gameId},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
         </trim>
    </insert>

    <update id="updateStaVideo" parameterType="StaVideo">
        update sta_video
        <trim prefix="SET" suffixOverrides=",">
            <if test="fileName != null">file_name = #{fileName},</if>
            <if test="filePath != null">file_path = #{filePath},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteStaVideoById" parameterType="Long">
        delete from sta_video where id = #{id}
    </delete>

    <delete id="deleteStaVideoByIds" parameterType="String">
        delete from sta_video where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>